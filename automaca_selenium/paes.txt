
daf
def
tif
    texto_resposta['text'] = times_sorteados
    
    
janela = Tk()

janela.title("Sorteador de times")

texto = Label(janela, text="Clique no botão para ver sortear")
texto.grid(column=0, row=0, padx=10, pady=10)

botao = Button(janela, text="Sortear times", command=sort_times)
botao.grid(column=0, row=1, padx=10, pady=10)

texto_resposta = Label(janela, text="")
texto_resposta.grid(column=0, row=2, padx=10, pady=10)


janela.mainloop()